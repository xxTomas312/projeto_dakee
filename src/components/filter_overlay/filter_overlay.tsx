import React, { useState } from 'react';
import { Link } from 'react-router-dom';

function filter_overlay_component() {
    const [aberto, setAberto] = useState(false);
    
    return(
        <>
        <h1>filter_overlay component</h1>
        </>
    )
}

export default filter_overlay_component;